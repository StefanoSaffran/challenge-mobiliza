import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  margin-left: 2.4rem;
  position: relative;
  cursor: pointer;

  > img {
    width: 4.8rem;
    height: 4.8rem;
  }
`;
